# vim: ts=2 sw=2 et

accounts:
  test:
    filters:
      arnold:
        commands:
          move: w00t
          add_flags:
            - '\Flagged'
        rules:
          - and:
            - from:
              - '.*arnoldbechtoldt.com'
      twitter:
        commands:
          move: w00t
        rules:
          - and:
            - subject:
              - Twitter
      w00tfilter:
        commands:
          move: w00t
        rules:
          - or:
            - subject:
              - w00t
      test:
        commands:
          move: test
        rules:
          - or:
            - subject:
              - test
      template:
        commands:
          move: w00t
        rules:
          - or:
            - subject:
              - template
      spam:
        commands:
          move: Test
            #add_flags:
              #- '\SEEN'
              #- '\Flagged'
        rules:
          - or:
            - subject:
              - filterme
