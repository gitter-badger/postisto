# vim: ts=2 sw=2 et

accounts:
  local_imap_server:
    connection: {}
    filters:
      main:
        commands:
          move: MyTarget
        rules:
          - and:
            - from:
              - "@youth4work.com"
